name: diesel_devtools_extension
description: DevTools extension UI for the diesel_dart inspector.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  devtools_extensions: ^0.5.0
  devtools_app_shared: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
